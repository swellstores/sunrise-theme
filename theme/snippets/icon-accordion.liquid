{%- if icon != 'none' -%}
  {%- assign file = icon | replace: '_', '-' | prepend: 'icon-' | append: '.svg' -%}
  <span class="svg-wrapper w-5 h-5">{{ file | inline_asset_content }}</span>
{%- endif -%}
